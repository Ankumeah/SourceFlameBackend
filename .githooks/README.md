This dir contains githooks to be used while working on this project

To use the hooks in this dir run the given command from the repo root
> [!NOTE]
> To make use of the given hooks, the `just` binary is required
```sh
git config core.hooksPath .githooks
```
