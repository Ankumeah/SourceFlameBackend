apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config-map
data:
  API_VERSION: "v1"
  BACKEND_PORT: "5000"
  BACKEND_HOSTNAME: "backend-service"
  JWT_LIFESPAN: "15m"
  TOKEN_TIMEOUT: "720h"
  TOKEN_LIMIT: "10"
  DATABASE_MAX_CONNS: "2"
  DATABASE_MAX_IDLE_CONNS: "2"
  DATABASE_MAX_LIFETIME: "10h"
  DATABASE_MAX_IDLE_TIME: "2h"
