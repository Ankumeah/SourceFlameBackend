user default off
user USERNAME on >PASSWORD ~refresh:* +GET +SETEX +DEL +PING +CLUSTER +INFO

cluster-enabled yes
cluster-config-file nodes.conf
cluster-node-timeout 5000

appendonly yes
protected-mode no
bind 0.0.0.0

maxmemory 1024MB
maxmemory-policy allkeys-lfu
save ""
